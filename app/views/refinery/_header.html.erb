<h1 id='logo'>
  <%#= link_to Refinery::Core.site_name, refinery.root_path %>
  <%= image_tag("logo.png") %>
</h1>
<div class="hero-unit">
  <% announcement = Refinery::Page.where(:slug => "announcement").first %>
  <%= raw announcement.content_for(:body) if announcement %>
</div>
<%= render(:partial => "/refinery/menu", :locals => {
             :dom_id => 'menu',
             :css => 'menu'
           }) %>
